[pytest]
# Research scripts in phase0/ are named *_test.py and execute at import;
# collecting them from the repo root re-runs locked-split evaluations and
# rewrites their report files. The suite lives in tests/ only.
testpaths = tests
