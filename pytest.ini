[pytest]
testpaths = tests
addopts = -q --tb=short
markers =
    slow: longer integration or live-artifact checks (skip with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning:sklearn.*
    ignore::FutureWarning:sklearn.*
    ignore::DeprecationWarning:joblib.*
    ignore::UserWarning:sklearn.*
